{
  "name": "@firefox-cli/protocol",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "format": "biome format .",
    "lint": "eslint .",
    "test": "vitest run src/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --pretty false --noEmit"
  }
}
