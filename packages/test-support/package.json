{
  "name": "@firefox-cli/test-support",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "format": "biome format .",
    "lint": "eslint .",
    "test": "vitest run src/**/*.test.ts",
    "typecheck": "bun run --cwd ../.. tsc -- -p packages/test-support/tsconfig.json --pretty false --noEmit"
  }
}
