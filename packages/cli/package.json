{
  "name": "@firefox-cli/cli",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@firefox-cli/native-host": "workspace:*",
    "@firefox-cli/protocol": "workspace:*"
  },
  "scripts": {
    "format": "biome format .",
    "lint": "eslint .",
    "test": "vitest run src/**/*.test.ts",
    "typecheck": "bun run --cwd ../.. tsc -- -p packages/cli/tsconfig.json --pretty false --noEmit"
  }
}
