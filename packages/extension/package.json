{
  "name": "@firefox-cli/extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@firefox-cli/protocol": "workspace:*"
  },
  "scripts": {
    "format": "biome format .",
    "lint": "eslint .",
    "test": "vitest run src/**/*.test.ts",
    "typecheck": "bun run --cwd ../.. tsc -- -p packages/extension/tsconfig.json --pretty false --noEmit"
  }
}
