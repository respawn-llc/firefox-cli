# firefox-cli

`firefox-cli` is a Firefox WebExtension plus CLI for controlling the user's main Firefox session from a terminal.

The repository is in planning/bootstrap. Read these files before implementing features:

- `AGENTS.md`
- `docs/firefox-cli-spec.md`
- `docs/implementation-plan.md`
